language: php

php:
  - "7.1"
  - "7.2"

matrix:
  allow_failures:
    - php: "7.2"
  fast_finish: true

before_script:
  - curl -s http://getcomposer.org/installer | php -- --quiet
  - php composer.phar install --dev --prefer-source

script: bin/phpunit
